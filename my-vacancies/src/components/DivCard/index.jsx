// eslint-disable-next-line react/prop-types
export const DivCard = ({ children1, children2 }) => {
    return (
        <div>
            <h2>{ children1 }</h2>
            <p>{ children2 }</p>
        </div>
    )
}